db-uri = "postgres://postgres@localhost/sitrep_test"
db-schema = "public" # this schema gets added to the search_path of every request
db-anon-role = "postgres"
db-pool = 10

server-host = "127.0.0.1"
server-port = 3000
